# Состояние архитектуры

Актуально на 2026-09-24 по статическому чтению `Z:/Godot/Projects/a_star/`.

Этот файл отделяет действующие контракты от планов. Он обновляется после архитектурных изменений.

## Реализовано

| Возможность | Текущее подтверждение в коде |
| --- | --- |
| Независимое ядро боя | `BattleEngine`, `BattleState`, команды и события не зависят от сцены |
| Атомарное создание боя | `BattleSetupFactory` и `BattleSessionFactory` возвращают ошибку без частичного запуска |
| Read-only граница | `BattleSession` возвращает `UnitSnapshot` и копию `HexGrid` |
| Общий путь игрока и ИИ | Оба источника создают `BattleCommand` и вызывают `BattleSession.step()` |
| Произвольная карта | `BattleMapDefinition` и `HexGrid` хранят явное множество axial-гексов |
| Изменяемая карта | `MapMutationService` атомарно применяет add/remove/change/set traversal и меняет ревизии |
| Headless-бой | `SimulationRunner` использует обычную сессию, лимиты и cancellation token |
| Скорость presentation | `BattlePlaybackSettings` и `BattlePresentationQueue` обновляют активные Tween |
| Пакеты контента | `ContentPackage`, manifests, зависимости, `ContentLoader`, `ContentSnapshot`, `ContentLock` |
| Способности | `AbilityDefinition`, `AbilityExecutor`, effect handlers и `ModAPI` |
| Кампания из боёв | `CampaignSession` и `CampaignSimulationRunner` выполняют переходы сценариев |
| Пользовательский редактор | Поле и маркеры, hex/placement-команды, undo/redo, JSON save/load, validation и trial battle |
| Разделение логики и визуала юнита | `UnitDefinition` с `presentation_id`; отдельный `UnitPresentationDefinition` и fallback actor |
| Локализация presentation | Отдельные display settings, hex-state renderer и unit-view registry |

## Реализовано частично

| Область | Что есть | Чего ещё нет |
| --- | --- | --- |
| Моды | Внутренние Resource-пакеты, dependencies и `ModAPI` | Установка и загрузка внешнего JSON/ZIP/PCK-пакета |
| Экспорт редактора | `manifest.json` и `battle_document.json` | Обратный импорт экспортированного каталога через `ContentLoader` |
| Кампания | Бои и переходы победа/поражение | Глобальная карта, Фобос/другие базы, сложные условия, постоянный состав и сохранения |
| Юниты | Здоровье, ход, базовая атака, способности | Статусы, cooldown, экипировка, травмы, полноценные stat modifiers и инициатива |
| Редактор | Карта, гексы, расстановки, стороны, AI, save/load/export/trial | Полный UI целей, зон, триггеров, кампаний, глобальной карты и диалогов |
| Симуляция | Один бой и последовательность кампании | Batch-runner, агрегаты, JSON/CSV серии и command log |
| Presentation | События, active Tween speed, локальные visual fallback | Универсальный skip/cancel всех операций, звук и единый clock для каждого VFX |
| ContentLock | Версии и стабильный fingerprint IDs | Хеш полного содержимого и строгая проверка байтовой идентичности |

## Актуальные ограничения и долги

- `BattleMapAssembler` больше не используется основным debug-запуском и остаётся legacy-адаптером. Новые зависимости на него не добавляются; удаление требует отдельной проверки миграционных сценариев.
- `BattleController` остаётся крупным графическим координатором выбора действия и AI presentation-loop. Правила в нём не размещать; выделять новый контроллер только при появлении отдельной причины изменения.
- `BattleEditorShell` объединяет сборку UI и workflow. Файловые сервисы, renderers и новые доменные правила следует выносить, а не наращивать shell.
- `UnitPresentationDefinition` сейчас входит в общий `ContentSnapshot`, но логика боя его не читает. Headless-запуск обязан оставаться корректным без использования графических ресурсов.
- `ContentLoader` работает с доверенными Godot `Resource`; это не безопасный data-only загрузчик внешних архивов.
- Текущий порядок ходов формируется порядком расстановок. Инициатива ещё не моделируется.
- Удаление занятого гекса и превращение его в непроходимый сейчас отклоняются.
- Сохранение посреди боя не принято как требование.

## Открытые игровые решения

Не решать эти вопросы случайно при работе над UI или контентом:

- инициатива и равные значения;
- cooldown и длительность статусов;
- стаки и сопротивления;
- перемещение после атаки;
- последствия удаления занятого гекса;
- многоклеточные существа;
- перенос травм и ресурсов между сценариями;
- точка сохранения/восстановления после поражения.

## Ближайшие архитектурные направления

Порядок определяется конкретной следующей фичей, но безопасные границы такие:

1. Для внешних модов — import/migration/validation конвейер поверх текущего `ContentSnapshot`.
2. Для развития редактора — расширять `EditorDocument` и `EditCommand`, не хранить данные только в UI-нодах.
3. Для кампании — отделить постоянное `CampaignSession` от определений и добавить SaveService с ContentLock.
4. Для глубины боя — вводить runtime-состояния способностей/статусов в ядре, не в `UnitActor`.
5. Для балансировки — добавить batch-runner и отчёты поверх одного `SimulationRunner`.
6. Для skip/pause — определить отменяемый контракт presentation до добавления кнопки.

## Аудит прежней папки «Архитектура»

| Прежний документ | Результат аудита | Каноническая замена в проекте |
| --- | --- | --- |
| `Архитектура v2.md` | Основное направление верно; план реализации и дерево папок устарели | `README.md` и этот файл |
| `Архитектура сцены боя v2.md` | Инварианты верны; часть рекомендуемых нод/контроллеров не существует и не обязательна | `BATTLE.md` |
| `Архитектура юнитов v2.md` | Разделение данных верно; статусы, cooldown и постоянный CharacterState пока цель | `BATTLE.md` и `STATUS.md` |
| `Контент, редактор и моды v1.md` | Целевой внешний формат верен; документ смешивал реализованное и будущее | `CONTENT_AND_MODS.md` и `EDITOR.md` |
| `Авторинг боя в редакторе v1.md` | Основной workflow уже реализован; расширенный авторинг остаётся целью | `EDITOR.md` |
| `Симуляция и скорость боя v1.md` | Базовый runner и speed реализованы; batch-report и skip остаются целью | `SIMULATION_AND_PLAYBACK.md` |
| `Оценка архитектуры проекта — 2026-09-09.md` | Устарел: перечисленные отсутствующими подсистемы теперь в основном существуют | `STATUS.md` |

Все действующие архитектурные решения из прежней папки перенесены в project-level документы. Новая документация не содержит Obsidian-ссылок и не зависит от существования той папки.

## Граница проверки

Статически сверены классы, публичные методы, content packages и текущие project-level документы. В рамках этой актуализации Godot не запускался, тесты не добавлялись и не выполнялись. Интерактивный UX редактора, визуальное совпадение и runtime-поведение этим документом не подтверждены.
